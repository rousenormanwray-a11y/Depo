# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ['nodejs-20_x']

[phases.install]
cmds = [
  'npm ci',
  'npx prisma generate'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx prisma migrate deploy && node dist/server.js'
